import { createEvent } from 'effector';

export const addApple = createEvent();
export const deleteApple = createEvent();
export const reset = createEvent();

// export const addAppleAndOrange = createEvent();